# Dotfiles

This repo contains some basic setup that I use for my environment. The idea is to create a set of zsh related configuration files that are generally useful for for myself (and perhaps others).
